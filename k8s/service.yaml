apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: autoflow
spec:
  selector:
    app: catalog-service
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
      name: http
  type: ClusterIP
